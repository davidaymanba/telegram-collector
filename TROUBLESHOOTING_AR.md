# استكشاف الأخطاء

## فشل `health-check`

- تأكد من وجود `config/channels.yaml`.
- تأكد من وجود `config/subjects.yaml`.
- تأكد أن اسم قناة تيليجرام يبدأ بـ `@` أو `https://t.me/`.
- تأكد أن أكواد المواد فريدة ومكتوبة بأحرف وأرقام ورموز آمنة مثل `DB101`.

## ظهور أسرار في السجلات

تمت إضافة حجب تلقائي لأي حقول تحتوي كلمات مثل `password` أو `secret` أو `api_key` أو `api_hash` أو `session` أو `otp`. إذا أضفت حقلا حساسا باسم مختلف في المراحل القادمة، يجب إضافته إلى قائمة الحجب في `app/logging/setup.py`.

## خطأ Telegram FloodWait

النظام يحترم المدة التي يطلبها تيليجرام وينتظر قبل إعادة المحاولة. لا تقلل `TUC_TELEGRAM_REQUEST_DELAY_SECONDS` إلى قيم عدوانية. الأفضل تشغيل الجمع بالتتابع وبمعدل محافظ.

## فشل OCR

- تأكد من تثبيت `tesseract-ocr`.
- تأكد من تثبيت `tesseract-ocr-ara`.
- تأكد أن `TUC_OCR_LANGUAGE=ara+eng`.
- الملفات ذات الجودة الضعيفة قد تتحول إلى `unclassified` بدلا من تصنيف خاطئ.

## الملفات duplicate

يعتمد التكرار على SHA-256 لمحتوى الملف، وليس الاسم. إذا تم رفع نفس الملف باسم مختلف، سيظهر كـ `duplicate` ولن يخضع لـ OCR أو AI مرة أخرى.

## `collect` لا يجمع رسائل قديمة

هذا مقصود. النظام يحفظ `last_message_id` لكل قناة ويجمع الرسائل الأحدث فقط. للاختبار الأول استخدم قناة صغيرة أو قاعدة بيانات جديدة.

## تداخل cron

الأوامر تستخدم قفل داخل التطبيق، وملف `deploy/cron.example` يستخدم `flock` أيضا. إذا ظهر أن القفل موجود، فهذا يعني أن تشغيل سابق لا يزال يعمل أو توقف بشكل غير طبيعي.
